# Copy to .env on the prod server at /opt/apps/logto-docker/.env
# Generate a strong random password (e.g. openssl rand -hex 32).
POSTGRES_PASSWORD=

# Optional: pin Logto to a specific version instead of :latest
# LOGTO_IMAGE=svhd/logto:1.23.0
